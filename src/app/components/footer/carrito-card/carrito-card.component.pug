ion-icon.button-dismiss(name="close-circle" '(click)'='dismiss()')
.left
  .image-container
    .image-round
      img('[src]'='imgUrl')
    .button-box
      .button.button--purple
        p('(click)'='disminuir()') -
        p {{cantidad}}
        p('(click)'='aumentar()') +
.right
  .price
    p ₡{{price}}
  .info
      span {{name}} | {{productor}}
